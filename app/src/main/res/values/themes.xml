<resources>
    <!-- Base application theme. -->
    <style name="Theme.Ine" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="Primary">@color/InePrimary</item>
        <item name="PrimaryAlpha">@color/InePrimaryAlpha</item>
        <item name="PrimaryDark">@color/InePrimaryDark</item>
        <item name="Accent">@color/IneAccent</item>
        <item name="Background">@color/Background</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/Background</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Jingburger" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="Primary">@color/JingburgerPrimary</item>
        <item name="PrimaryAlpha">@color/JingburgerPrimaryAlpha</item>
        <item name="PrimaryDark">@color/JingburgerPrimaryDark</item>
        <item name="Accent">@color/JingburgerAccent</item>
        <item name="Background">@color/Background</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/Background</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Viichan" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="Primary">@color/ViichanPrimary</item>
        <item name="PrimaryAlpha">@color/ViichanPrimaryAlpha</item>
        <item name="PrimaryDark">@color/ViichanPrimaryDark</item>
        <item name="Accent">@color/ViichanAccent</item>
        <item name="Background">@color/Background</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/Background</item>
        <!-- Customize your theme here. -->
    </style>
</resources>